{
  "name": "precommit-detect",
  "description": "Read-only check for pre-commit setup state. Detects missing pre-commit binary, uninstalled .git/hooks/pre-commit, and unreachable Docker daemon when the config requires it. Walks you through approval-gated installs via SessionStart and PostToolUse:EnterWorktree hooks.",
  "version": "0.1.0",
  "author": {
    "name": "Taylor Leese",
    "email": "tleese22@gmail.com"
  }
}
